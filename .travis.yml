language: cpp
dist: bionic
services:
    - docker

before_install:
    # Install zig
    - sudo snap install zig --classic --edge

    # Pull wrk
    - docker pull williamyeh/wrk

script:
    # Test individual files
    - zig test -OReleaseSafe lib/zhp/status.zig
    - zig test -OReleaseSafe lib/zhp/headers.zig
    - zig test -OReleaseSafe lib/zhp/middleware.zig
    - zig test -OReleaseSafe lib/zhp/mimetypes.zig
    - zig test -OReleaseSafe lib/zhp/time/datetime.zig
    - zig test -OReleaseSafe lib/zhp/util.zig
    - zig test -OReleaseSafe lib/zhp/request.zig
    - zig test -OReleaseSafe lib/zhp/response.zig
    - zig test -OReleaseSafe lib/zhp/forms.zig


    # Make sure this repo builds
    - zig build -Drelease-safe=true install

    # Test example server
    - zig run tests/bench.zig
